# 中文 PPT 与网页设计调研笔记

> 面向的场景：用 AI 生成**简体中文**的 PPT（以 HTML 网页形式实现）和网页。
> 调研时间：2026 年 8 月。
> 与同目录下另两份文档的分工：
> - `数据卡片设计经验手册.md`：讲「一张竖版图文卡片」怎么做好（字号梯度、截断检测、导出）。
> - `排版调研笔记-中英文与载体.md`：讲「中英文差异与载体差异」下的换行与排版决策。
> - **本文件**：讲「HTML 版 PPT」和「网页」这两个新载体的规范、硬数字、中文审美与雷区。

---

## 怎么用这份文档

1. **不要拘泥于下面的任何一条规则。** 规则是给你的先验，不是给你的镣铐。当内容本身、品牌调性或用户的明确要求与某条规则冲突时，按「内容与用户优先」处理，可以适当变通，也可以引入文档里没写过的组件和版式。
2. **但坑要绕开。** 文档里标注为「硬约束」「有出处」「会坏掉」的条目，属于工程事实或国家标准，不是审美偏好。能遵守就遵守。典型的比如：导出 PDF 前必须勾 Background graphics、`font-weight` 在中文上超过 700 会糊、Web 中文字体必须做分片。
3. **数字都是可查的。** 每条关键数字后面都标了出处章节，见文末「来源清单」。凡是查不到出处的说法，一律进「打假清单」，不要当成事实往外讲。
4. **本文档可以直接交给其他 AI 助手使用。** 语言已按「给别人看」的标准组织，不依赖本次对话的上下文。
5. **没查到的就写「没查到」。** 不要用 plausible 的数字填空，尤其在要交付给别人的文档里。

---

## 目录

- [零、三条总纲](#零三条总纲)
- [一、HTML 版 PPT](#一html-版-ppt)
  - [1.1 画幅：先定像素，再定比例](#11-画幅先定像素再定比例)
  - [1.2 安全边距](#12-安全边距)
  - [1.3 信息密度](#13-信息密度)
  - [1.4 字号换算](#14-字号换算)
  - [1.5 视觉与动效](#15-视觉与动效)
  - [1.6 框架怎么选](#16-框架怎么选)
  - [1.7 工程约束与导出](#17-工程约束与导出)
  - [1.8 雷区清单](#18-雷区清单)
- [二、网页设计](#二网页设计)
  - [2.1 栅格：12 栏是惯例，不是标准](#21-栅格12-栏是惯例不是标准)
  - [2.2 间距尺度](#22-间距尺度)
  - [2.3 行宽：中英文换算会骗人](#23-行宽中英文换算会骗人)
  - [2.4 断点：按内容选，不按设备选](#24-断点按内容选不按设备选)
  - [2.5 可访问性硬数字](#25-可访问性硬数字)
  - [2.6 性能硬数字](#26-性能硬数字)
  - [2.7 中文 Web Font](#27-中文-web-font)
  - [2.8 雷区清单](#28-雷区清单)
- [三、中文审美与视觉习惯](#三中文审美与视觉习惯)
  - [3.1 信息密度：更简单，但不要极简](#31-信息密度更简单但不要极简)
  - [3.2 色彩语义：红涨绿跌不是「国标」](#32-色彩语义红涨绿跌不是国标)
  - [3.3 色彩数量与配色公式](#33-色彩数量与配色公式)
  - [3.4 移动端生态：不要只做手机](#34-移动端生态不要只做手机)
  - [3.5 字号与可辨识下限](#35-字号与可辨识下限)
  - [3.6 交互与信息架构](#36-交互与信息架构)
  - [3.7 中文排印硬规则](#37-中文排印硬规则)
- [四、AI 生成物的特有雷区](#四ai-生成物的特有雷区)
- [五、可执行规则速查](#五可执行规则速查)
- [六、打假清单](#六打假清单)
- [七、来源清单](#七来源清单)

---

## 零、三条总纲

在进入细节之前，先把三件事定下来，后面所有的取舍都从这三件事推导。

**第一条：载体决定单位。** PPT 的单位是「画面高度」，因为观众坐在几米外看你的一整屏；网页的单位是「CSS 像素 + 视口宽度」，因为读者贴在 30 厘米外滚动。同一个 32px 的字，在 1280×720 的 PPT 里是标题级，在 1440px 宽的网页里只是正文偏大。

**第二条：中文不是「窄一点的英文」。** 汉字撑满 em 方框，拉丁字母只占其中一部分，所以中文的视觉密度天生高于英文。所有从英文语境搬来的经验（行宽 45～75 字符、Material 的 40～60 字符、`font-weight` 的 100～900 全梯度）在中文上都要重新换算，直接套用会得到偏窄的栏宽和糊成一团的字重。

**第三条：先确定「不能坏」，再谈「好不好看」。** 导出 PDF 时背景丢失、中文字体没分片导致首屏 3 秒白屏、字重 800 糊成黑块，这些是「坏了」；配色不好看只是「不够好」。工程约束优先于审美优化。

---

## 一、HTML 版 PPT

用 AI 做 PPT 时，如果你采用「生成一个 HTML 幻灯片页面」这条路（reveal.js / Slidev / Marp 或者纯手写），下面这些是和传统 PowerPoint 不一样的地方。

### 1.1 画幅：先定像素，再定比例

PowerPoint 的「16:9」是物理尺寸（13.333 × 7.5 英寸），而 HTML 的世界里没有英寸，只有像素。**各家框架的默认画布并不统一，直接抄 CSS 会翻车。**

| 工具 / 环境 | 默认画布 | 宽高比 | 备注 |
| --- | --- | --- | --- |
| PowerPoint（Widescreen） | 13.333 × 7.5 in | 16:9 | 物理尺寸，不是像素 |
| reveal.js | 960 × 700 | ≈ 1.371:1 | **既不是 16:9 也不是 4:3**，这是最容易被忽略的一个 |
| Slidev | 980 × 551.25 | 16:9 | `canvasWidth: 980`，`aspectRatio: 16/9` |
| Marp / Marpit | 1280 × 720 | 16:9 | 官方默认 |
| Google Slides（16:9） | ≈ 960 × 540 | 16:9 | 未在文档中给出精确像素值 |
| Keynote | 未公布像素值 | 16:9 / 4:3 | Apple 不公开画布像素 |

**实践建议：**

- **手写 HTML 幻灯片时，用 1280 × 720。** 它是 Marp 的默认值，也是「16:9 + 整数像素 + 720p 同源」三个条件同时满足的唯一选择。后续所有字号按这个基准换算。
- 如果你用 reveal.js 且不改配置，你的画布是 960×700（1.371:1），投到 16:9 投影仪上会有黑边或拉伸。**要么显式配置尺寸，要么接受这个比例并在设计时按 1.371:1 构图。**
- 画布一旦定了，**所有元素用百分比或 `em`/`rem` 表达，不要用硬编码 px**，这样换画布只改根字号一处。
- 4:3 已经基本退出历史舞台，除非明确知道投放环境是老投影仪，否则统一 16:9。

### 1.2 安全边距

reveal.js 的默认 `margin` 是 `0.04`，即**画布短边的 4%**。1280×720 下约为 28.8px，960×700 下约为 28px。

这个数字的来源是「投影仪过扫描（overscan）」时代的老经验：一些投影设备会裁掉画面边缘 2%～5%。现代数字投影和 LED 屏基本不过扫描，但保留 4% 边距仍然有用，因为：

- 它天然形成了一个「内容框」，避免文字贴边导致的拥挤感；
- 多页之间边距一致，翻页时视觉不跳；
- 导出 PDF 或图片时不会因为 1px 的抗锯齿被切掉笔画。

**建议起步值：画布短边的 4%～6%。** 信息密度高的页面取下限，标题页/章节页可以放到 8%～10% 甚至更多，用留白强调这一页的特殊性。

### 1.3 信息密度

先说一个前提：中文页面可以比英文页面**密一些**，但「密」指的是信息条目多，不是**字小**。字号由观看距离决定，和这一节说的内容量是两件事。

**ANSI/AVIXA V202.01:2026 DISCAS**（Display Image Size for 2D Content，前身是 ANSI/INFOCOMM V202.01:2016）

**先说清楚它是干什么用的：DISCAS 是「屏幕该多大、最远观众能坐多远」的标准，不是字号标准。** 它按内容类型给出「最远观看距离 ≤ 画面高度 × N」：

| 观看任务 | 定义 | 最远观看距离 |
| --- | --- | --- |
| 分析型（Analytical） | 需要判读细节，如财务报表、密集数据表 | 画面高度 × 4 |
| 基础型（Basic） | 一般演示、讲授 | 画面高度 × 6 |
| 被动型（Passive） | 只看大字标题、氛围型画面 | 画面高度 × 8 |

读法：**内容越需要细看，允许的观看距离越近**（×4）；只放标题的氛围页可以远到 ×8。网上常把 4/6/8 直接当成字号规则引用，那是误读，1.4 节会补上缺的那一步。

**Colin Purrington 的学术会议海报指南**（colinpurrington.com/tips/poster-design/，作者个人博客，长期维护，被广泛引用）

它给的是**内容量**的约束，不是字号：

- 全文总字数 **800～1200 词**，目标是 **1000 词或更少**（正文约 700 词 + 所有图表图注约 300 词），对应「5～10 分钟读完」；
- 标题 **1～2 行**；
- 文本框每行 **45～65 个字符**（是字符，不是单词）；
- 图表的细节、图注、坐标轴标签和数字，都要能在 **6 英尺（约 1.8 米）外**舒适观看；
- 关键内容（如 Results）放在**中央可见区**，次要内容（文献引用、致谢）降级到底部；
- 不要因为版式是竖的，就把重要内容放到需要弯腰才能看的低处。

最后一条迁移到 PPT 上的意义很直接：**重要内容不要放在画面底部**，那一段在真实会场里最容易被人挡住。

**Duarte（《slide:ology》）的 3 秒规则**：一页幻灯片的「要点」应该在 3 秒内被读懂。这不是说一页只能放 3 秒的信息量，而是说**观众的第一眼要有一个明确的落点**。如果第一眼看过去是一整屏等权重的文字，这一页就是失败的。

**Garr Reynolds（《Presentation Zen》）和 Cliff Atkinson（《Beyond Bullet Points》）** 的共同主张：幻灯片是给讲者用的提示，不是给读者看的文章。所以「一页一个论点」比「一页三个要点」更符合幻灯片的媒介属性。

**对你的实践意义：** AI 生成 PPT 时有两个常见的极端。一个是**把每个二级标题都拆成一页**，20 分钟的内容做出 45 页；另一个是把一页塞成一篇短文。**页数应该由内容量和讲述时间决定，不由标题层级决定。**

关于「20 分钟该做多少页」，流传最广的说法是 16 页左右，但**这个数字查不到可靠出处**，已列入打假清单（第六章）。作为粗略的计划锚点可以用，但不要当成依据，更不要写进交付文档里当成事实。

### 1.4 字号换算

**先记住一件事：决定「看不看得清」的不是 pt 或 px，而是「字符高度 ÷ 观看距离」。** 同一个 24pt 的字，贴在 1 米外看很大，投在 10 米外看就是一团。所以任何脱离观看距离谈字号的规则都要打折看待。

**DISCAS 只给了距离，没给字号。** 它说的是「最远观众 ≤ 画面高 × 6」，要变成字号还缺一步：字符在视网膜上的视觉角度。补上这一步有两条路。

**第一条路：从第一方数据出发，按距离缩放。**

Genigraphics 的官方印前说明（genigraphics.com/fileprep）给出了明确数字，而且**标注了校准距离**，这在同类资料里很少见：

| 元素 | 官方建议 | 校准距离 |
| --- | --- | --- |
| 正文 | 最小 **28pt**，理想 **32～40pt** | **4～5 英尺（约 1.2～1.5 米）**可读 |
| 标题 | **≥ 72pt** | |
| 作者及单位 | **40～60pt** | |
| 分节标题 | 48pt（作为「同类元素用同一字号」的示例） | |
| 参考文献 | **≤ 12pt** | |

它还给了一个比任何公式都可靠的自检方法：**把文件按 100% 显示（缩放模板用 200%），然后退后 4～5 英尺看。** 这个方法值得直接照搬到 PPT 上（见本节末尾）。

注意这个 28pt 是**近距阅读**的标准。放映场景的观看距离远得多，不能直接搬，必须放大。

**第二条路：从视觉角度推算。**

- 汉字的**字面高度**（视觉上黑白部分的高度）大约占 em 框的 **72%～80%**，其余是行间空隙；
- 中文字符的笔画复杂度高于拉丁字母，**同等条件下需要更大的视觉尺寸**。《Displays》2024 年一篇针对虚拟环境中中文字符可辨识性的实验研究（*Displays*, 81, 102589）给出的方向性结论是：字符复杂度越高，所需的最小可辨识尺寸越大，因此不能把基于拉丁字母的字号标准直接套到中文上（该文具体阈值在付费全文里，这里只引用方向性结论）；
- 综合观看距离（DISCAS 的 ×6）与上述中文修正，放映场景下**正文高度 ≈ 画面高度的 3%～5%** 是一个可用的区间。**这是推算，不是某份标准里的原文。**

代入 1280×720（画面高 720px）：

- 下限 720 × 3% ≈ **22px**
- 上限 720 × 5% ≈ **36px**

**落到实操上：1280×720 的中文 PPT，正文默认 32px（≈ 24pt）。** 小会议室（最远观众 3～4 米）可以降到 28px；报告厅、大会议室（最远观众 8 米以上）要提到 36～40px。**因为中文需要比同等信息量的英文更大的视觉尺寸，取值一律靠这个区间的中上段，不要取下限。**

配套的梯度建议（1280×720 基准）：

| 层级 | 字号 | 字重 | 行高 | 说明 |
| --- | --- | --- | --- | --- |
| 封面主标题 | 72～96px | 600～700 | 1.15 | 上限见下方字重说明 |
| 章节页标题 | 56～64px | 700 | 1.2 | |
| 页面标题 | 40～48px | 700 | 1.25 | |
| 正文 / 要点 | **28～40px，默认 32px** | 400～500 | 1.5～1.6 | 中文行高要比英文高 |
| 注释 / 图注 / 来源 | 20～24px | 400 | 1.5 | 低于 20px 在投影上会读不清 |

> **最终验证靠实测，不靠公式。** 按你预计的屏幕尺寸把页面等比投出来（或用全屏预览模拟），然后**走到你预计的最远座位上看一眼**。网上流传的「最小 pt ≈ 最远距离 ÷ 8」这类公式，各家给的除数从 6 到 12 都有，属于经验区间，不要当成精确依据。

**字重上限 700。** 中文字重在 800 及以上时，笔画间的白空间被填掉，标题会糊成黑块，这个现象在小字号、低分辨率投影、以及导出为 JPEG 后尤其明显。**中文 PPT 只用 400 / 500 / 600 / 700 四档。**

还有一条容易忽略的：**系统里的中文字体往往只有 2～3 个真实字重**（比如苹方常见 Regular / Medium / Semibold）。你写了 `font-weight: 500`，浏览器要么就近匹配，要么合成伪粗体，结果和你在设计稿里看到的不是一回事。所以字重**取值越少越安全**，同一份 PPT 里建议只用 2～3 档。

### 1.5 视觉与动效

**动画的两个硬约束：**

1. **WCAG 2.1 SC 2.3.3「Animation from Interactions」（AAA 级）**：由交互触发的非必要动画应当可以被关闭。W3C 在 `prefers-reduced-motion` 的说明中**明确点名了翻页动画（page-flipping animation）** 作为需要响应该偏好的例子。
2. 所以，任何「整页翻转 / 整页推拉 / 视差滚动」的入场效果，都应该包在：

```css
@media (prefers-reduced-motion: no-preference) {
  /* 只有用户没有要求减少动效时才启用 */
}
```

而**不是**反过来写 `@media (prefers-reduced-motion: reduce) { *{animation:none} }`。前者的语义是「默认不给动效，用户允许才给」，对前庭功能障碍用户更友好，也更符合标准的本意。

**动效的合理用法：**

- 动效用来表达「关系」，不用来表达「炫技」。两个元素之间用动效连接，观众能看懂因果；整页元素无差别地依次淡入，只会拖慢节奏。
- 一页之内的入场元素控制在 3～5 个。超过这个数量，观众会把注意力放在「下一个是什么」而不是「这是什么」。
- `opacity: 0 → 1` 配 `translateY(20px) → 0`、缓动 `ease-in-out`、子元素用固定 0.1s 递增延迟，这套组合是 AI 生成的默认模板（见第四章），**识别度极高，建议换成非等差的延迟或直接分批出现。**

**深色背景在 PPT 上的特殊性：**

- PPT 通常投影或在大屏显示，**深色底 + 浅色字在明亮会议室里对比度反而不如白底黑字**，因为环境光会抬高投影的黑色电平。
- 如果一定要深色底，把「黑」控制在 `#12121A` 这类带一点色相的深灰而不是纯 `#000`，字色用 `#F5F5F7` 而不是纯 `#fff`（纯白在深色底上会有光晕）。

### 1.6 框架怎么选

| 框架 | 定位 | 适合 | 不适合 |
| --- | --- | --- | --- |
| **reveal.js** | 纯前端幻灯片库，生态最老 | 需要自定义交互、嵌套幻灯片、演讲者视图、Markdown 编写 | 对打印/PDF 的控制较弱（见 1.7） |
| **Slidev** | 基于 Vite + Vue 的「开发者向」幻灯片 | 要写代码高亮、要跑 live demo、要 Vue 组件 | 官方明确说明：**交互功能在导出文件中不起作用**；导出 PPTX 本质上是逐页截图 |
| **Marp / Marpit** | 「一个可以通过打印转成 PDF 的网页」 | 追求稳定、要快速产出 PDF、内容以静态图文为主 | 官方自述为静态 HTML + CSS，无运行时逻辑，因此**没有入场动画、没有演讲者视图** |

**选型建议：**

- **交付物是 PDF 或图片** → 优先 Marp 或纯手写静态 HTML。它的「所见即所得」程度最高，导出最不容易出问题。
- **交付物是要现场演示的网页** → Slidev 或 reveal.js。
- **AI 生成的场景** → 建议生成**静态 HTML + 少量 CSS 动画**，不要依赖框架的运行时特性。理由：AI 生成的代码里最容易出错的恰恰是框架配置和插件注册，而静态 HTML 的失败模式是「不好看」而不是「打不开」。

### 1.7 工程约束与导出

这一节是「会坏掉」的部分，请逐条落实。

**1. `print-color-adjust` 默认会吃掉你的背景。**

MDN 明确说明，`print-color-adjust` 的默认取值是 `economy`，意味着**浏览器可以在打印时自行决定丢弃背景色和背景图**，以省墨。你在屏幕上看到的深色渐变页，导出 PDF 后可能变成纯白底上的黑字，而文字色如果是浅色就彻底看不见了。

解法：在任何需要保留背景的元素上显式声明：

```css
.slide {
  -webkit-print-color-adjust: exact;
  print-color-adjust: exact;
}
```

**2. reveal.js 导出 PDF 必须勾 Background graphics。**

reveal.js 官方 PDF 导出流程第 6 步要求在打印对话框里勾选「Background graphics（背景图形）」。不勾的话，上面这条 `print-color-adjust` 的坑必然踩中。

**3. reveal.js 的 PDF 导出只在 Chromium 系浏览器上可用。** 官方说明中这是明确的限制。用 Safari 或 Firefox 导出会遇到布局和分页问题。

**4. 打印样式的分页控制。**

转 PDF 时每一页应该独立成页，不要出现「一页 PPT 被切成两半」：

```css
@page { size: 1280px 720px; margin: 0; }
.slide { break-after: page; page-break-after: always; }
.slide:last-child { break-after: auto; page-break-after: auto; }
```

**5. 导出前自查清单：**

- [ ] 深色背景页加了 `print-color-adjust: exact`
- [ ] 打印对话框勾了 Background graphics
- [ ] 用 Chromium 系浏览器导出
- [ ] 逐页翻过一遍，确认没有半页截断
- [ ] 所有 `<img>` 都加载完成（未完成的图片导出后是空白）
- [ ] 如果有动画，确认导出时元素处于「动画结束态」而不是「`opacity:0` 的初始态」
- [ ] 如果用了非系统字体，确认已经内嵌或转曲（换一台电脑打开会 fallback）
- [ ] 导出前已给容器加上终态类，动画元素不是 `opacity: 0`

最后两条是最常见的翻车点：

**6. 换台设备字体就没了。** 这是 PowerPoint 时代的老坑，在 HTML 版 PPT 上一样存在，只是表现不同：你用了「思源宋体」或某个品牌定制字体，传到别人的电脑或另一台演示机上，系统里没有这个字体，浏览器会静默回退到默认字体，你的行宽、断行、字重全部走样，而且**不会报错**。三种解法，按可靠性排序：

- **转曲 / 导出成 PDF 或图片**（最稳，但文字不可再编辑、不可复制、也不利于无障碍）；
- **用系统字体栈**（见 2.7），跨平台一致性最好；
- **Web 字体内嵌 + `font-display: block`**，但中文字体动辄数 MB，且离线环境下可能加载失败。

另外，HTML 版 PPT 如果要发给别人，建议**连同字体文件一起打包**，或者干脆交付 PDF。

**7. 入场动画没跑完就导出。** 入场动画如果用 `opacity: 0` 做初态，导出 PDF 时动画还没跑，整页就是空的。**导出前先把动画强制置为终态**，比如给容器加一个 `.exported` 类，把所有元素拉回终态：

```css
.exported .anim { opacity: 1 !important; transform: none !important; }
```

这一条和「字体 fallback」属于同一类问题：**静默失败**。不报错，导出完你以为没事，到现场才发现整页是空的。

### 1.8 雷区清单

**雷 1：画布比例混用。** 一半页面按 16:9 构图，一半按 reveal.js 的 1.371:1 构图，翻页时画面在跳。先定尺寸，再画内容。

**雷 2：字重 800/900。** 中文糊成黑块。上限 700。

**雷 3：正文低于 28px。** 1.4 节给的默认是 32px，28px 已经是小会议室场景的下限。再往下，坐第三排以后就读不清了。

**雷 4：整页文字无差别等权重。** 观众第一眼没有落点，违反 3 秒规则。每页给一个明确的视觉主角。

**雷 5：把 PPT 当 Word 用。** 一页塞满一段完整的文章。幻灯片是提示，文章是文档，两者用不同的密度标准。

**雷 6：入场动画没做 `opacity` 终态兜底。** 导出 PDF 后整页空白。

**雷 7：深色底没加 `print-color-adjust`。** 导出后背景丢失，浅色字消失。

**雷 8：在 Slidev 里做了交互，然后指望它在导出的 PDF 里能用。** 官方明确说不行。

**雷 9：章节之间不做视觉区分。** 20 页长得一模一样的幻灯片，观众不知道讲到哪了。至少给章节页一个不同的版式或底色。

**雷 10：图表用了英文缩写且不加注释。** 中文观众看到 "MoM / YoY / ARPU" 需要额外一次转换。要么写「环比 / 同比 / 每用户平均收入」，要么在首次出现时给中文注释。

---

## 二、网页设计

### 2.1 栅格：12 栏是惯例，不是标准

「12 栏栅格」之所以流行，一个现实原因是 12 能被 2、3、4、6 整除，组合灵活。**但它从来不是任何标准组织的规定**，历史上 960 Grid System 同时提供了 12 栏和 16 栏两套。

主流设计系统的实际取值：

| 设计系统 | 栏数 | 官方给出的理由 |
| --- | --- | --- |
| Ant Design 5 | **24** | 官方表述：多数业务场景需要在设计区域内解决大量信息存储问题 |
| Carbon（IBM） | 16 | |
| Fluent 2（Microsoft） | 12 | |
| Material 3（Google） | 无固定栏数 | 改用 pane / column / ruler 三元模型，不要求统一栏数 |
| Apple HIG | 无 Web 栅格规范 | 只提供平台级布局指导 |

**Ant Design 选 24 栏的理由值得单独记一笔**：它是中文业务场景下最主流的 Web 设计系统，而它选 24 栏的动机是「信息量大」。这与第三章「中文用户容忍更高信息密度」的结论是同一个现象的两个侧面。**做中文后台、数据看板、信息密集页面时，24 栏比 12 栏更顺手。**

**选择建议：**

- 营销页 / 内容型网站 / 作品集：12 栏足够，栏数少意味着更容易做出明确的主次关系。
- 后台 / 仪表盘 / 电商列表页：用 24 栏（Ant Design 体系）或 16 栏（Carbon 体系）。
- 移动端：4 栏（Ant Design 的做法）。

### 2.2 间距尺度

| 设计系统 | 基准单位 | 常用值 |
| --- | --- | --- |
| Material 3 | 8dp | 4 / 8 / 12 / 16 / 24 / 32 / 48 |
| Ant Design | 8px | 亲密性原则下的纵向间距三档：**8 / 16 / 24px**（小 / 中 / 大） |
| Carbon | 2 / 4 / 8 的倍数 | 层级更细 |
| Fluent 2 | **4px** | 比 Material 更细 |

**中文场景的注意点：** 8px 基准是从英文 UI 的组件尺寸推出来的。中文界面里文字块的视觉重量更大，**在小间距（4px、8px）上更容易显得挤**。实践中的做法是：组件内部用 4px 或 8px 基准，**区块之间（section 之间）额外加一档**，比如列表项之间 16px、卡片组之间 24px、大区块之间 48～64px。

用 CSS 变量统一管理：

```css
:root {
  --sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px; --sp-4: 16px;
  --sp-6: 24px; --sp-8: 32px; --sp-12: 48px; --sp-16: 64px;
}
```

### 2.3 行宽：中英文换算会骗人

这是中文排版里最容易踩的一个坑，值得展开讲。

**英文的经典结论：**
- Bringhurst《The Elements of Typographic Style》§2.1.2：**45～75 个字符一行**，理想值 66。
- Material 3 官方：**40～60 个字符**。

**为什么中文不能直接用：**

拉丁字母的平均宽度约为 **0.5em**（小写字母更窄，但含空格后平均下来接近 0.5em）；汉字是等宽的全角字符，**1 个字符 = 1em**。

同样按「40～60 字符」计算：

| 语言 | 40 字符 | 60 字符 |
| --- | --- | --- |
| 英文 | ≈ 20em | ≈ 30em |
| 中文 | 40em | 60em |

在 16px 字号下换算成像素：

| 语言 | 推荐行宽 |
| --- | --- |
| 英文 | **320～480px** |
| 中文 | **640～960px** |

**差了整整一倍。** 如果你把「Material 说 40～60 字符」直接套到中文上，会得到一条窄得离谱的正文栏。

**中文的通行说法是「一行 30～40 个汉字」**，但这个说法**查不到权威出处**（见打假清单）。从上面 45～75 的换算反推，中文合理的行宽应该是 **45～75 个汉字**，30～40 偏窄，更适合卡片内的短描述而不是长正文。

**实践建议：**

- 中文长正文：**一行 35～50 个汉字**（取 Bringhurst 区间的中下段，因为汉字密度高，同样字数视觉负担更大）。
- 用 `ch` 单位时要注意：`1ch` 是字符 "0" 的宽度，在等宽字体和比例字体下差异很大，**中文界面里不要依赖 `ch` 控制行宽**，直接用 `max-width` 配合 `em`：

```css
.prose {
  font-size: 16px;
  line-height: 1.75;              /* 中文行高高于英文 */
  max-width: 42em;                /* ≈ 42 个汉字 ≈ 672px */
}
```

### 2.4 断点：按内容选，不按设备选

各家断点值：

| 系统 | 断点（px） |
| --- | --- |
| Material 3 | <600 / 600–839 / 840–1199 / 1200–1599 / 1600+ |
| Ant Design 5 | 576 / 768 / 992 / 1200 / 1600 / 1920 |
| Bootstrap 5.3 | 576 / 768 / 992 / 1200 / 1400 |
| Tailwind CSS v4 | 640 / 768 / 1024 / 1280 / 1536 |
| Fluent 2 | 320 / 480 / 640 / 1024 / 1366 / 1920 |
| Carbon | 最大内容宽度 1584px（99rem） |

**Material 3 的官方态度是「Design for breakpoints instead of specific devices」**（按断点设计，而不是按具体设备设计）。Bootstrap 5 的文档也说自己的断点「代表了一部分常见设备尺寸的子集」，并不是为某个具体设备定制的。

**实践建议：**

- **不要背设备。** 不要写「iPhone 是 390px，所以断点设在 390」。设备尺寸每年变，断点应该是「我的布局在哪个宽度开始崩」的实测结果。
- **起步用 3 个断点就够：** 640（手机横屏 / 小平板竖屏）、1024（平板横屏 / 小笔记本）、1440（桌面）。大多数中文站点在这个粒度下足够。
- 断点的做法是：**先做窄屏，逐步加宽，在布局开始难看的地方加一个断点**，而不是先做宽屏再往回缩。

**容器查询（Container Queries）** 自 2023 年 2 月起进入 MDN Baseline「Widely Available」。它解决的是「组件根据自身容器宽度响应」的问题，**但它不取代页面级断点**。两者是不同层级：页面级用媒体查询，组件级用容器查询。

### 2.5 可访问性硬数字

以下全部来自 WCAG 2.2，可以直接作为验收标准。

| 条款 | 级别 | 要求 |
| --- | --- | --- |
| **1.4.1 使用颜色** | A | 不能仅用颜色传达信息（红绿色盲约占男性人口的 8%） |
| **1.4.3 对比度（最小值）** | AA | 正文 **4.5:1**；大文本 **3:1**（大文本定义为 **18pt 或 14pt 粗体**；W3C 明确给了 CJK 的对应说明） |
| **1.4.4 调整文本大小** | AA | 放大到 **200%** 不丢失内容与功能 |
| **1.4.10 回流（Reflow）** | AA | 在 **320 CSS 像素**宽度下不出现二维滚动 |
| **1.4.11 非文本对比度** | AA | 图标、图形、控件边界 **3:1** |
| **1.4.12 文本间距** | AA | 行高 **1.5**、段间距 **2em**、字间距 **0.12em**、词间距 **0.16em**，全部可调而不丢内容 |
| **1.4.13 悬停或焦点上的内容** | AA | 悬停/焦点触发的内容必须可关闭、可悬停、且持续存在 |
| **2.5.5 目标尺寸（增强）** | AAA | **44 × 44** |
| **2.5.8 目标尺寸（最小值）** | AA | 点击目标 **24 × 24 CSS 像素**（有 Spacing 例外：若目标间距足够，24px 直径圆不重叠即可） |
| **3.3.1 错误识别** | A | 错误必须以文本形式明确标识 |

**白底上的灰色文字对比度参考（自己算的，可用作快速判断）：**

| 色值 | 对比度（对 #fff） | 能否过 AA 正文 |
| --- | --- | --- |
| `#999999` | ≈ 2.85:1 | ❌ 不能 |
| `#CCCCCC` | ≈ 1.61:1 | ❌ 不能 |
| `#767676` | ≈ 4.54:1 | ✅ 刚好过线 |
| `#595959` | ≈ 7.0:1 | ✅ 过 AAA |

**记一条就够：白底上的辅助文字不要浅于 `#767676`。** 很多 AI 生成的页面喜欢用 `#999` 或 `#ccc` 做次要文字，那是不达标的。

**触摸目标尺寸，各家设计系统：**

| 系统 | 触摸 | 指针（鼠标） | 最小间距 |
| --- | --- | --- | --- |
| Material 3 | 48 × 48dp | 44 × 44dp | ≥ 8dp |
| Apple HIG | 44 × 44pt | — | — |
| Fluent 2 | iOS & Web 44×44；Android 48×48 | — | — |

**中文场景的额外一条：** WCAG 2.5.8 的 24×24 是**下限**。中文按钮里的文字比英文多（「立即注册」vs "Sign up"），按钮本身就更大，所以实际很少会卡在这条上。**容易卡的是图标按钮和表格行内的操作链接**，那里往往只有 16×16 的图标。

**关于居中长段落的一个澄清：** WCAG **并没有任何条款禁止居中长段落**。1.4.8（AAA 级，视觉呈现）只规定了「行宽不超过 80 个字符」和「不使用两端对齐（justified）」。居中长段落难读是排版经验，不是无障碍违规，不要混为一谈。

### 2.6 性能硬数字

Core Web Vitals 的「良好」阈值，取**第 75 百分位**，移动端和桌面端分别统计：

| 指标 | 含义 | 良好 |
| --- | --- | --- |
| **LCP** | 最大内容绘制 | ≤ 2.5 秒 |
| **CLS** | 累计布局偏移 | ≤ 0.1 |
| **INP** | 下次绘制交互（已取代 FID） | ≤ 200 毫秒 |

**中文字体是 LCP 的头号杀手**，见 2.7。CLS 的常见来源是「图片没写宽高」和「Web Font 加载完成后的 FOUT 重排」。

### 2.7 中文 Web Font

一套完整的中文字体（GB18030 覆盖面）动辄 5～10MB，整包加载必然拖垮 LCP。

**Google Fonts 的做法（2018 年官方博客公布）** 是「按使用频率分片」：

- 使用频率最高的前 2000 个汉字 → 第 1 片；
- 接下来的 1000 个汉字 → 第 2 片；
- 其余汉字 → 按 Unicode 码点范围切成 100 片；
- 浏览器通过 `unicode-range` 只下载页面实际用到的那些片。

效果：**中文字体体积减少约 88%**；韩文改进后在此基础上再减 38%；日文约 80%。

**实践建议：**

- **优先用系统字体栈。** 对于中文界面，系统字体（苹方、微软雅黑、思源黑体的系统版本）在各自的平台上就是最优选择，而且零加载成本：

```css
--sans: system-ui, -apple-system, "Segoe UI", "PingFang SC",
        "Hiragino Sans GB", "Microsoft YaHei", "Source Han Sans SC",
        "Noto Sans CJK SC", sans-serif;
```

- **如果必须用自定义中文字体，一定要做 `unicode-range` 分片**（可以用 `fonttools` 的 `pyftsubset`，或直接使用 Google Fonts / 字节、阿里等 CDN 提供的分片版本）。
- 配 `font-display: swap` 和 `size-adjust`，减少字体切换造成的 CLS。
- **标题字少、正文字多。** 只在标题上用自定义字体、正文用系统字体，是一个高性价比的折中。

### 2.8 雷区清单

**雷 1：`#999` / `#ccc` 做正文或辅助文字。** 对比度 2.85:1 和 1.61:1，都不达 AA。下限 `#767676`。

**雷 2：整包加载中文字体。** 5～10MB，LCP 直接爆表。要么用系统字体栈，要么做 `unicode-range` 分片。

**雷 3：图片不写宽高。** CLS 超标。写 `width`/`height` 属性或 `aspect-ratio`。

**雷 4：只做移动端。** CNNIC 第 57 次报告显示，桌面设备使用率 32.5%、笔记本电脑 31.2%、平板电脑 31.6%（多项可选，所以加起来超过 100%），这批人是真实存在的。

**雷 5：把英文的行宽建议直接套到中文。** 一字之差，行宽差一倍。见 2.3。

**雷 6：信息只在 hover 时展现。** 违反 WCAG 1.4.13，而且移动端根本没有 hover。

**雷 7：只用颜色表示状态或错误。** 违反 1.4.1 和 3.3.1。错误提示必须有文字。

**雷 8：点击目标小于 24×24。** 图标按钮是重灾区。

**雷 9：断点照抄设备尺寸。** 断点应该来自「布局在哪崩」，不是「iPhone 多宽」。

**雷 10：正文行高用英文的 1.4。** 中文视觉密度高，正文中建议 1.7～1.8，最低不低于 1.6。

---

## 三、中文审美与视觉习惯

这一章的证据强度不一，我在每条后面都标注了来源性质。请按证据强度决定采信程度。

### 3.1 信息密度：更简单，但不要极简

**来源：NN/g（Nielsen Norman Group）2016 年研究，Yunnuo Cheng 与 Jakob Nielsen，报告名《Are Chinese Websites Too Complex?》。**

- 方法：12 名参与者（6 位中国用户 + 6 位在中国生活的外籍用户），8 个网站，出声思维法。
- **性质：定性研究，n=12，不能当作量化结论推广。**

主要发现：

- 中国用户对**高信息密度的容忍度高于西方用户**，并且**明确排斥低密度的极简设计**。
- 研究中一位用户的原话很能说明问题：「这个站设计很好……但问题在于无用信息是没了，有用信息也没了，我什么都找不到。」
- NN/g 给出的处方是：**「更简单，但不要过度简化或极简」（simpler, but not oversimplified or minimalist）。**

**配套证据：** Ant Design 把栅格设为 24 栏，官方理由就是「多数业务场景中需要在设计区域内解决大量信息存储问题」。这条与上面的用户研究是同一个现象的两个侧面。

**对你的实践意义：**

- 做中文页面时，**不要为了「高级感」把信息压到只剩三个卡片加一句 slogan。** 中文用户会把这解读为「这个站没什么内容」。
- 高密度的前提是**有组织**。用户容忍的是「密集但分类清晰」，不是「密集且混乱」。分栏、分组、区块标题、分隔线这些组织手段，在高密度页面上是必需品。
- 留白依然重要，但它的作用应该是**分组**而不是**减少内容量**。用留白把信息块切开，而不是把信息删掉。

**一个重要的边界：** 这一段讲的是**工具类、信息类、商业类**页面。**品牌形象页、作品集、活动主视觉**不受此约束，那类页面的目标本来就是「少即是多」。先判断页面属于哪一类，再决定密度。

### 3.2 色彩语义：红涨绿跌不是「国标」

**红涨绿跌的来历（多方交叉验证后的通行说法）：**

- 中国大陆早期**跟随国际惯例，用绿色表示上涨、红色表示下跌**。
- 转折点是券商引入中国台湾的「乾隆」系列行情软件，该软件采用红涨绿跌，随软件普及成为行业习惯。
- **1992 年中国证监会成立时，并没有就此出台过任何行政规定。** 行业内的普遍说法是「纯粹是技术上的事情」。
- 因此，「为了区别于社会主义市场而改成红色」是**以讹传讹的坊间说法**。

**不同市场的实际用法：**

| 市场 | 涨 | 跌 |
| --- | --- | --- |
| 中国大陆（证券行情） | 红 | 绿 |
| 中国台湾 | 红 | 绿 |
| 日本 | 红 | 绿（另有其他变体） |
| 韩国 | 橙 / 红 | 蓝 |
| 中国香港 | 绿（沿用英国习惯） | 红 |
| 俄罗斯 | 白 / 蓝 | 红（另有其他变体） |

**一个容易忽略的细节：中国大陆的外汇报价仍然沿用绿涨红跌。** 也就是说，同一个中国用户，在股票软件里看到红是涨，在银行 App 的外汇页面里看到绿是涨。**两套语义在中国大陆是并存的。**

**实践建议：**

- 金融类界面里，**不要只靠颜色**，要配「▲ / ▼」或「+ / −」符号。
- 如果你的产品同时涉及证券和外汇，考虑在相应模块里显式说明或提供切换。
- 面向国际用户的产品，不要用红绿作为唯一的状态编码（红绿色盲约占男性人口的 8%）。

### 3.3 色彩数量与配色公式

**关于「中国人喜欢大红大金」：这是一个刻板印象，研究结论相反。**

**来源：清华大学美术学院 祝诗扬、宋立民、宋文雯，《基于移动端的中国人色调偏好研究》，《设计》2022 年第 19 期，149–152 页。**

结论：中国人偏好的色调排序是 **黑白灰 ＞ 艳 ＞ 亮 ＞ 浅**。**无彩色（黑白灰）是最大公约数**，高饱和色的偏好排在后面。

**正确的配色公式：中性色打底 + 高饱和色做强调。** 也就是大面积用黑白灰和低饱和色建立秩序，用小面积高饱和色承担「重点」「行动点」「状态」这三个功能。

**关于色彩数量，有明确的官方规范可依：**

**来源：国办发〔2017〕47 号《政府网站发展指引》附件《网页设计规范》。**

- 主色调 **1 种**，全站总色数 **不超过 3 种**；
- 信息排布遵循「**从左到右、从上到下**」；
- 时间格式为 `YYYY—MM—DD HH∶MM`（注意里面用的是一字线和比号）；
- 避免浮动、闪烁的元素；
- 严禁刊登商业广告；
- 栏目数量建议 **5～8 个**。

这份规范是给政府网站定的，但它的约束（色数、栏目数、动效克制）对**所有中文商业站点都有参考价值**，因为它反映的是中文用户对「秩序感」的期待。

**中文场景的几条具体建议：**

- **不要全站大面积铺高饱和色。** 白色或极浅灰做底，深色做文字，一个品牌色做强调，这是最不容易出错的中文配色结构。
- **深色模式要给「黑」加点色相。** 纯 `#000` 在中文字幕/正文下对比过于生硬，用 `#121212`～`#1A1A1F` 这一档。
- **数据可视化不要用「彩虹配色」。** 需要多色区分时，用同一色相的明度阶梯（如 Ant Design 的 blue-1 到 blue-10），或者用一个中性色 + 一个强调色。

### 3.4 移动端生态：不要只做手机

**来源：CNNIC 第 57 次《中国互联网络发展状况统计报告》（2026 年 2 月 5 日发布，数据截至 2025 年 12 月）。**

| 指标 | 数值 |
| --- | --- |
| 网民规模 | **11.25 亿** |
| 互联网普及率 | **80.1%** |
| 手机网民 | **11.21 亿（99.6%）** |
| 农村网民 | 3.14 亿（27.9%） |
| 网络支付 / 社交 / 短视频等 | 社交 11.14 亿（98.9%）、短视频 10.74 亿（95.4%）、网络购物 9.37 亿（83.2%） |
| 上网设备 | 台式机 32.5%、笔记本电脑 31.2%、平板 31.6%（多选，合计 > 100%） |

**两条推论：**

1. **手机是绝对主力，但不是全部。** 三个非手机设备都在 31%～33% 之间，意味着大约三分之一的网民会在大屏上使用你的页面。「只做移动端」会实实在在地丢掉用户。
2. **响应式是默认要求，不是加分项。**

**微信小程序的设计基线已经更新：** 官方指南现在给出的设计基准是 **375px（固定）或 390px（响应式）**。**「750rpx / iPhone 6 基准」是过时的说法**，不要再沿用。

小程序官方的字号梯度：**22 / 17 / 15 / 14 / 12 pt**（从页面大标题到辅助说明）。Tab 栏最少 2 个、最多 5 个，**建议不超过 4 个**。右上角胶囊菜单不可自定义，设计时要在它右侧留出空间。点击目标 **7～9mm**。

**微信 X5 内核的已知坑（来自社区总结，官方没有发布过完整的黑名单）：**

- iOS 微信内 `<video>` 即使加了 `muted` 也不会自动播放，必须在用户手势的同步调用栈里执行 `play()`；
- 旧版 X5 对 `flex: 1`、`align-items: stretch`、`flex-wrap` 的支持有问题；
- 微任务相关的缺陷会导致请求被静默挂起；
- 滥用 `will-change` 和 3D transform 会导致白屏或 GPU 崩溃。

**长按识别二维码的实操要求：** 二维码必须是独立的 `<img>` 元素，四周留白，不要和其他可点击元素重叠。**没有官方的最小像素值**，实践中 200×200 CSS 像素以上比较稳。

### 3.5 字号与可辨识下限

**中文界面的主流字号基准：14px。**

- **Ant Design**：主字号 **14px**，行高 **22px**；提供 10 级字号梯度，官方建议**一个系统内只使用 3～5 级**；正文对比度建议达到 **7:1 以上（AAA）**。
- **Arco Design（字节跳动）**：字号取值为 12 / 13 / 14（正文）、12（说明）、16 / 20 / 24（标题）、36 / 48 / 56（展示级）；字重提供 100～900（**中文仍然建议不超过 700**）；间距尺度严格是 **4 × N px**；组件高度 mini 24 / small 28 / default 32 / large 36；圆角 none / small 2 / medium 4 / large 8 / circle 50%；主色 `primary-6`，配 1～7 级色阶（5 为 hover，7 为点击）；文字色 `text-1` 到 `text-4` 四级；数据可视化提供 **20 个数据色**（`data-1` 到 `data-20`）。

**中文正文字号不要低于 12px。** 12px 是 Ant Design 和微信小程序共同的下限，再小在普通屏幕上就开始糊了。

**关于「最小可辨识字号」的研究：**

**来源：陆永盛等，《中老年移动端数字阅读最小可辨识字号研究》，《包装工程》2022 年第 43 卷第 16 期，DOI 10.19554/j.cnki.1001-3563.2022.16.027。**

测得的最小可辨识字号为：弱黑 **7.17pt**、深灰 **7.17pt**、灰色 **8.19pt**。

**值得注意的结论是：颜色变浅会抬高可辨识的字号下限。** 灰色需要比深色更大的字才看得清。这条和 2.5 节的对比度要求是同一件事的两种表述。

**注意这条研究的边界：** 样本是中老年群体，不能推广到全体用户。它的价值在于给出「最不利情况」的参考，而不是给你一个通用的下限。

### 3.6 交互与信息架构

- **导航栏栏目数：** 政府网站规范建议 5～8 个；微信小程序 Tab 建议不超过 4 个。中文站点的导航普遍比英文站点层级更深、入口更多，这是信息密度偏好的自然结果。
- **首页要有「入口密度」。** 中文用户的典型行为是从首页直接寻找目标，而不是逐级下钻。首页应该承担「目录」的角色。
- **不要用 hover 承载唯一入口。** 移动端没有 hover，而且违反 WCAG 1.4.13。
- **搜索框在中文站点的重要性高于英文站。** 因为信息密度高、入口多，搜索是主要的效率工具。
- **表单校验要按中文的实际规则来。** 这是中文 Web 特有的一块，照搬英文站的表单会直接不可用：
  - **手机号**：中国大陆是 11 位、1 开头，不要用通用的「数字 + 长度」校验；国际区号要单独处理。
  - **姓名**：不要限制「只能英文字母」，也不要假设「姓 + 名」两个字段（这个拆法本身就是英文习惯）。中文场景通常只用一个「姓名」字段。
  - **身份证号**：18 位，最后一位可能是 X，要允许大写 X 并做大小写归一化。
  - **地址**：不要用「街道 / 城市 / 州 / 邮编」的四级结构硬套，中文地址的行政层级是省 / 市 / 区 / 详细地址。
  - **输入长度**：用字符数而不是字节数计算（UTF-8 下一个汉字是 3 字节，按字节算会把长度算成 3 倍）。
  - **输入法 composition**：中文输入有「拼音组词」的中间态，绑定 `input` 事件做实时校验或实时搜索时，要用 `compositionstart` / `compositionend` 包一层，否则会在用户还没选字时就触发。

### 3.7 中文排印硬规则

这一节是**国家标准**，是「必须」而不是「建议」。

**1. 横排文本用弯引号 “” 和 ‘’，不用直角引号 「」『’。**

**GB/T 15834-2011《标点符号用法》4.8.2 和 5.2.3 明确规定：横排文稿使用弯引号 “” 和 ‘’。**

直角引号 「」和『』在标准里是**竖排文稿**使用的（竖排时写作 ﹃﹄ 和 ￢﹂）。在横排中文里使用 「」 是中国香港、中国台湾、日本的习惯。**这是简体中文内容里最常被搞错的一条。**

顺带一句：单引号用在双引号内部，即「他说：“这就是‘那个’东西。”」这个嵌套关系。

> **关于本文档的体例：** 正文里凡是「引用词语、举例子、标注说法」的地方，统一用直角引号 「」 作为**引用符号**；这与「正文内容里该用哪种引号」是两件事。按上面的规则，你做出来的中文页面（标题、正文、按钮、图注）一律用 “” 和 ‘’，不要用 「」。


**2. 中英文之间加不加空格：国家标准没有规定。**

**GB/T 15834-2011 没有任何关于中英文之间加空格的条款。** 所以「国标要求加空格」是假的。

存在 CY/T 154-2017 这项标准涉及相关排版规范，但其条文内容无法直接查证。W3C 的 CLReq（中文排版需求）文档给出的建议是「**不多于四分之一个汉字宽**」，但那是在竖排排版的语境下讨论的。

**实践建议：** 这是排版风格问题，不是规范问题。**选一种，全篇统一。** 目前中文互联网的通行做法是加一个半角空格，视觉上确实更清楚。

**3. 数字用法（GB/T 15835-2011《出版物上数字用法》）：**

| 规则 | 例子 |
| --- | --- |
| 概数用汉字，两数之间不加顿号 | 三四天、五六十人（不写「三、四天」） |
| 已定型的词语用汉字 | 一律、星期五、一方面、十进制 |
| 千分撇可用，但年份、页码、标准号**不分节** | 12,500 可以；2026 年不写 2,026；GB/T 15834 不写 GB/T 15,834 |
| 年份不能简写 | 不写「26 年」代替「2026 年」 |
| 数值范围中百分号**不可省略** | 15%～30%（不写「15～30%」） |

**4. 标点的占位规则：**

- 中文标点**占一个汉字宽度**；
- 大陆标准把标点放在**字框的左下角**（中国香港、中国台湾放在正中）。这就是为什么中文标点和拉丁字母混排时，标点看起来位置偏低，这不是 bug；
- **破折号（——）占两个汉字宽度，且中间不能断行**；
- 省略号（……）同样占两个汉字宽度。

**5. 中文没有真正的斜体，不要用 `font-style: italic`。** 这是对中文最典型的误用之一。拉丁字母的斜体是一套独立设计的字形，而中文字体通常不带斜体；浏览器遇到 `font-style: italic` 会**把正体字强行倾斜（合成伪斜体 / synthesized oblique）**，结果是笔画变形、重心歪斜，在小字号下尤其难看。需要强调时，用下面这些方式代替：

- 加粗（**中文加粗是安全的**，但字重上限见 1.4）；
- 换颜色（注意不能只靠颜色传达信息，见 2.5 的 1.4.1）；
- 加着重号（`text-emphasis`，这是为中文设计的标准属性）；
- 加底色的 `<mark>` 或边框。

**6. 正文首行缩进不是必须的，Web 上更推荐用段间距。** 「中文段落首行缩进两字符」是**印刷和公文的传统**，来自纸质阅读时代靠缩进区分段落的习惯。Web 上没有这个必要，通行做法（包括 Ant Design、微信等中文产品）是**不缩进，用段间距分隔**。如果一定要缩进，用 `text-indent: 2em`（`2em` 正好是两个汉字的宽度，这是 `em` 单位在中文里最好用的地方之一）。

**7. 自动换行：`word-break: break-all` 是个陷阱。** 中文文本本身不需要 `break-all`（汉字天然可以任意断行），加上它反而会破坏英文单词和数字的完整性。正确的组合是：

```css
.prose {
  overflow-wrap: break-word;    /* 长的英文单词 / URL 才断 */
  word-break: normal;           /* 中文正常断行，不要 break-all */
  line-break: strict;           /* 严格避头尾 */
}
```

真正需要 `break-all` 或 `anywhere` 的场景只有一种：**连续的长串无空格内容**（长 URL、长 ID、Base64、日志），而且最好只作用在那一类元素上，不要加在全局。

**8. 字间距（letter-spacing）只加在标题上，正文不要加。** 中文标题加 0.02em～0.1em 的字间距是常见的美化手法，视觉上更「松」。但**正文加字间距会降低阅读效率**，因为汉字本身是等宽的方块，读者依赖这个稳定的节奏跳读，加了间距反而破坏了它。另外 `text-justify` 相关的两端对齐在中文 Web 上同样不建议用（WCAG 1.4.8 也明确不推荐 justified）。

**9. 避头尾：** 行首不能出现 `。，、；：？！）》」』` 等收尾标点和闭括号，行尾不能出现 `（（《「『` 等起始标点。这是中文排版的基本要求，现代浏览器的 `line-break: strict` 和 `overflow-wrap` 能处理大部分情况，但**标题和短文本的手动换行必须自己检查**。

---

## 四、AI 生成物的特有雷区

这一章讲的是「AI 生成的网页/PPT 一眼就能看出来是 AI 生成的」那些共同特征。

**先说证据强度。** 网络上流传的「AI slop（AI 泔水）」分析文章里，有一份流传较广的指南来自一家商业公司的博客。我对这份材料做了核查，结论是：

- **它的「模式清单」是可靠的**，因为这些模式在实际生成的页面里确实反复出现，肉眼可验证；
- **它的统计数据不可信。** 理由：该站点本身是售卖相关产品与服务的供应商；它用自己的扫描器给自家生成器打 24/100、给竞品打 92/100，属于自证；文章无署名、无外部引用、所有链接指向站内；文中「94%」「83%」等数字没有给出方法学；且存在内部矛盾（同一指标在文中给出 75～92 与 69～92 两个区间，前言说做了 6 次 v0 生成，正文说每个工具各 5 次）。

**所以：把下面这份清单当作「经验总结」，不要引用其中的百分比。**

### 视觉模式的雷

| 雷 | 具体表现 | 建议替代 |
| --- | --- | --- |
| **蓝紫渐变** | Tailwind 的 `blue-500` / `indigo-600` / `violet-500`，色相全部落在 **200°～290°** 区间；典型写法 `bg-gradient-to-r from-blue-600 to-indigo-700` | 换成有明确色相倾向的色（青、橙、墨绿），或用中性底 + 单一强调色 |
| **纯黑纯白文字** | 正文用 `#000` 或 `#fff` | 用 `#1A1A1F` / `#F5F5F7` 这类带一点色相的深/浅色 |
| **默认英文字体** | Inter / Poppins / Roboto / Montserrat / Geist / DM Sans | 中文页面直接上系统字体栈（见 2.7） |
| **统一的超大圆角** | 全站 `rounded-2xl`（16px） | 按层级区分：卡片 8～12px，按钮 6～8px，标签 4px 或全圆 |
| **卡片三连** | 三个等宽 `grid-cols-3` 卡片，结构完全一样 | 让三块的权重不同，或改用 2+1、1+2 的非对称布局 |
| **居中眉标题（eyebrow）** | 正中小胶囊标签 + 居中大标题 + 居中副标题 + 居中 CTA 按钮 | 至少一处改为左对齐，制造视觉锚点 |
| **统一的大间距** | 每个区块都是 `py-20` 或 `py-24` | 按内容亲疏调整间距，相关内容近、不同内容远 |
| **固定页面顺序** | nav → hero → features → testimonials → pricing → faq → cta → footer | 按真实需求删减，不要默认全上 |
| **入场动效三件套** | `opacity: 0` + `translateY(20px)` + `ease-in-out`，子元素固定 0.1s 递增 | 用非等差延迟，或改为分批出现；必须包在 `prefers-reduced-motion` 里 |
| **毛玻璃导航栏** | `backdrop-blur-md` 配半透明底 | 可以保留，但要给一个不透明兜底（`background: rgba(...,.85)`）。**中文场景额外注意**：微信内置浏览器（X5 内核）与部分旧安卓机对 `backdrop-filter` 支持不佳，兜底色不是可选项而是必选项 |
| **标题没做断行优化** | 缺少 `text-wrap: balance`（标题）或 `text-wrap: pretty`（正文） | 标题加 `text-wrap: balance`，正文加 `text-wrap: pretty` |

### 文案模式的雷

以下这些短语在 AI 生成的英文页面里高频出现，中文里也有对应的套路表达：

- "Welcome to our platform" / "Get started today" / "Unlock the power of"
- "Effortlessly" / "Seamlessly" / "Streamline your workflow"
- 中文对应物：「一站式」、「赋能」、「助力」、「打造」、「全新升级」、「尽享」

**处理原则：删掉所有不承载具体信息的副词和形容词。** 「无缝集成」和「集成」的信息量是一样的，前者只是更长。

### 结构模式的雷

- **每个区块都有标题 + 副标题 + 三段正文**，节奏完全一致；
- **数字和指标没有来源**，凭空出现「提升 300%」；
- **页面很长但信息量很低**，滚动三屏还没说清楚在做什么；
- **所有区块等权重**，没有主次。

**一个通用的自检方法：** 把页面缩到 25% 看，如果你分不出哪一块是重点，那读者也分不出。

---

## 五、可执行规则速查

把前面所有内容压缩成可以直接照着做的清单。

### HTML 版 PPT

```
画布        1280 × 720（16:9），所有尺寸用 % / em 表达
安全边距    画布短边的 4%～6%
正文字号    默认 32px；小会议室 28px，大会议室 36～40px
标题梯度    封面 72～96 / 章节 56～64 / 页标题 40～48 / 正文 28～40 / 注释 20～24
字重        只用 400 / 500 / 600 / 700，同一份里只用 2～3 档
字号验证    投出来，走到最远的座位上看一眼（公式不如实测）
行高        正文 1.5～1.6，标题 1.15～1.25
关键位置    重要内容不要放在画面底部，容易被挡
页数        由内容量与讲述时间决定，不要按标题层级拆页
动效        包进 @media (prefers-reduced-motion: no-preference)
导出        加 print-color-adjust: exact；勾 Background graphics；用 Chromium
导出兜底    先把所有入场动画强制置为终态
```

### 网页设计

```
栅格        内容/营销页 12 栏；后台/看板 24 栏；移动端 4 栏
间距        4 / 8 / 12 / 16 / 24 / 32 / 48 / 64（中文区块间额外加一档）
正文字号    14px（界面）/ 16px（阅读型），中文不低于 12px
行高        中文正文 1.7～1.8，界面文字 1.5
行宽        中文长正文一行 35～50 个汉字（用 max-width: 42em 表达）
断点        起步 640 / 1024 / 1440；按「布局在哪崩」实测调整
文字对比度  正文 ≥ 4.5:1，大文本 ≥ 3:1；白底辅助文字不浅于 #767676
点击目标    ≥ 24×24（AA）／≥ 44×44（AAA，推荐）
字体        优先系统字体栈；自定义字体必须 unicode-range 分片
性能        LCP ≤ 2.5s，CLS ≤ 0.1，INP ≤ 200ms
图片        必须写 width / height 或 aspect-ratio
```

### 中文特化

```
引号        横排用 “” 和 ‘’，不用 「」『』
中英空格    国标无规定，选一种全篇统一
数字        概数用汉字（三四天）；数值范围百分号不省（15%～30%）
            年份不简写；千分撇不用于年份/页码/标准号
标点        占一个汉字宽；破折号占两个且不断行；避头尾
斜体        中文没有真斜体，用加粗 / 颜色 / text-emphasis 代替，别用 font-style:italic
首行缩进    不是必须，Web 上通行做法是不缩进 + 段间距
自动换行    word-break: normal + overflow-wrap: break-word；只有长 URL / 长 ID 才用 break-all
字间距      只在标题上加，正文不要加
密度        容忍高密度，但必须有组织；不要极简到「什么都没有」
配色        主色 1 种、总色数 ≤ 3；中性打底 + 高饱和做强调
              「中国人爱大红大金」是刻板印象，研究显示黑白灰偏好排第一
设备        手机 99.6%，但台式机/笔记本/平板各占三成，不要只做移动端
小程序基线  375px（固定）或 390px（响应式）；字号 22/17/15/14/12 pt
            Tab ≤ 4 个；点击目标 7～9mm
```

---

## 六、打假清单

以下说法在中文设计圈子里流传很广，但**查不到可信出处**，不要当作事实引用。

| 说法 | 实际情况 |
| --- | --- |
| 「Web 栅格必须是 12 栏」 | 12 栏是惯例不是标准。960 Grid System 同时提供 12 和 16；Ant Design 用 24，Carbon 用 16，Material 3 干脆不用统一栏数 |
| 「中文行宽一行 30～40 字」 | 通行说法，查不到权威出处。从 Bringhurst 的 45～75 字符换算，中文合理区间应在 45～75 字；实践中建议 35～50 字 |
| 「PPT 的 6×6 法则（每页 6 行、每行 6 词）」 | 广泛流传，但**找不到权威来源**。可用（它确实能防止塞太多），但别说是「某权威规定」 |
| 「20 分钟的汇报应该做 16 页」 | **查不到可靠出处**。作为计划锚点可以用，但不要当成依据写进交付物 |
| 「PPT 正文最小 24pt」 | 这个数字本身有出处，但**脱离观看距离谈它没有意义**。Genigraphics 官方给的是「4～5 英尺阅读下正文最小 28pt、理想 32～40pt」，是近距标准；放映场景要按距离放大 |
| 「关键内容要放在画面顶部 90% 以内」 | **查不到出处**。方向是对的（底部容易被挡），但 90% 这个数字没有来源 |
| 「中文段落必须首行缩进两字符」 | 这是**印刷和公文的传统**，不是 Web 规范。中文 Web 产品（Ant Design、微信等）通行做法是不缩进、用段间距 |
| 「中文要用 `word-break: break-all`」 | 反了。中文天然可断行，不需要 `break-all`；它会破坏英文单词和数字。只有长 URL / 长 ID 这类连续无空格串才需要 |
| 「国标要求中英文之间加空格」 | GB/T 15834-2011 无此条款。CY/T 154-2017 存在但条文无法查证 |
| 「红涨绿跌是证监会规定的」 | 1992 年证监会成立时并无此行政规定。转捩点是券商引入中国台湾「乾隆」软件后的行业习惯 |
| 「中国网站比西方网站信息密度高 2～3 倍」 | **没有任何量化研究支持这个倍数**。NN/g 的研究是 n=12 的定性研究，只能说「密度容忍度更高」，给不出倍数 |
| 「中国人偏好大红大金」 | 刻板印象。清华美院 2022 年的研究显示色调偏好排序为 黑白灰 ＞ 艳 ＞ 亮 ＞ 浅 |
| 「F 型 / Z 型阅读模式（中文版）」 | F/Z 模式本身在西方也缺乏高质量重复验证；**针对中文的 F/Z 模式研究没有可信的重复实验**，不要断言 |
| 「AI 生成的网站有 94% 存在某某问题」 | 见第四章。这类数字出自商业供应商的自证材料，无方法学、无外部引用，且文中自相矛盾 |
| 「微信小程序基准是 750rpx / iPhone 6」 | 已过时。官方指南现给出 375px（固定）或 390px（响应式） |
| 「字号越大越易读」 | 只在下端成立。超过一定大小后，行宽和行高对阅读效率的影响更大 |
| 「深色模式更护眼」 | 缺少一致的研究结论。在明亮环境下，深色底浅色字的对比度表现通常不如白底黑字 |

---

## 七、来源清单

### 标准与规范

- **GB/T 15834-2011《标点符号用法》** —— 4.8.2、5.2.3（横排引号）
- **GB/T 15835-2011《出版物上数字用法》** —— 概数、年份、千分撇、数值范围
- **CY/T 154-2017** —— 相关排版规范，条文内容无法直接查证
- **ANSI/AVIXA V202.01 DISCAS**（现行版本 V202.01:2026）—— 4/6/8 观看距离规则
- **WCAG 2.1 / 2.2（W3C）** —— 1.4.1、1.4.3、1.4.4、1.4.10、1.4.11、1.4.12、1.4.13、2.3.3、2.5.5、2.5.8、3.3.1
- **国办发〔2017〕47 号《政府网站发展指引》附件《网页设计规范》** —— 色数、栏目数、时间格式、动效与广告
- **微信小程序官方设计指南** —— 375/390 基线、字号梯度、Tab 数量、点击目标

### 研究与报告

- **Nielsen Norman Group, "Are Chinese Websites Too Complex?"**（Yunnuo Cheng & Jakob Nielsen，2016）—— n=12 定性研究
- **祝诗扬、宋立民、宋文雯，《基于移动端的中国人色调偏好研究》，《设计》2022(19): 149–152**
- **陆永盛等，《中老年移动端数字阅读最小可辨识字号研究》，《包装工程》2022, 43(16)，DOI 10.19554/j.cnki.1001-3563.2022.16.027**
- **CNNIC 第 57 次《中国互联网络发展状况统计报告》**（2026-02-05 发布，数据截至 2025-12）
- **Bringhurst, *The Elements of Typographic Style*** —— §2.1.2，45～75 字符
- **Colin Purrington, "Designing conference posters"**（colinpurrington.com/tips/poster-design/）—— 总字数 800～1200 词、目标 ≤1000、正文 700 + 图注 300、标题 1～2 行、每行 45～65 字符、图表细节 6 英尺可读、关键内容放中央可见区
- **Genigraphics 官方印前说明**（genigraphics.com/fileprep）—— 正文最小 28pt / 理想 32～40pt（校准距离 4～5 英尺）、标题 ≥72pt、作者 40～60pt、参考文献 ≤12pt、「退后 4～5 英尺自检」
- **周效舟等，*Research on the legibility of Chinese display character sizes in virtual environments*，Displays, 2024, 81: 102589** —— 中文字符复杂度越高，所需最小可辨识尺寸越大；不能把拉丁字母的字号标准直接套用到中文（具体阈值在付费全文中，本文只引用方向性结论）
- **Duarte, *slide:ology*** —— 3 秒规则
- **Garr Reynolds, *Presentation Zen***
- **Cliff Atkinson, *Beyond Bullet Points***

### 设计系统

- **Material 3（Google）** —— 8dp 间距、pane/column/ruler 布局模型、断点 600/840/1200/1600、40～60 字符、48/44dp 目标尺寸
- **Ant Design 5（蚂蚁集团）** —— 24 栏栅格、8px 间距、14px 主字号、断点 576/768/992/1200/1600/1920
- **Arco Design（字节跳动）** —— 4×N 间距、字号梯度、data-1..data-20 数据色
- **Carbon（IBM）** —— 16 栏栅格、最大内容宽度 1584px
- **Fluent 2（Microsoft）** —— 12 栏栅格、4px 间距、断点 320/480/640/1024/1366/1920
- **Apple HIG** —— 44×44pt 目标尺寸（无 Web 栅格规范）

### 技术文档

- **MDN** —— `print-color-adjust` 默认值 `economy`；`prefers-reduced-motion`；容器查询 Baseline（2023 年 2 月 Widely Available）
- **reveal.js 官方文档** —— 默认画布 960×700、`margin: 0.04`、`minScale: 0.2`、`maxScale: 2.0`、PDF 导出流程（含 Background graphics 要求与 Chromium 限制）
- **Slidev 官方文档** —— `canvasWidth: 980`、`aspectRatio: 16/9`、导出限制（交互功能失效、PPTX 为逐页图片）
- **Marp / Marpit 官方文档** —— 默认画布 1280×720、静态 HTML + CSS 无运行时逻辑
- **Google Fonts 官方博客（2018）** —— CJK 字体分片策略与 88% / 80% / 38% 的体积收益
- **web.dev / Google** —— Core Web Vitals 阈值（LCP 2.5s、CLS 0.1、INP 200ms）
- **Bootstrap 5.3 / Tailwind CSS v4 文档** —— 断点值及其「代表性设备尺寸」的表述

### 存疑来源（仅供模式参考，不要引用数字）

- 某商业公司博客的「AI slop」分析文章 —— 模式清单可用；统计数据为厂商自证材料，无方法学、无署名、无外部引用，且存在内部矛盾

---

## 附：这份文档没有解决的问题

坦诚地列出边界，避免它被误用：

1. **中文网页的「美观度」没有量化标准。** 本文档给的是「不犯错」的底线（对比度、字号、密度、标点），以及「哪些做法大概率不好」的清单，但给不出一个「好」的公式。审美判断仍然要靠大量看图和实践。
2. **中文字体的版权边界没有覆盖。** 思源系列（SIL OFL）、阿里巴巴普惠体（免费商用授权）、站酷系列（各自授权不同）的授权条款差异很大，商用前必须逐个确认。这份文档没有展开。
3. **没有覆盖动效的具体参数（时长、缓动曲线）。** 各家设计系统（Material 3 的 emphasized/decelerated、Fluent 2 的曲线集）取值不同，且高度依赖品牌调性，没有通用答案。
4. **没有覆盖中文图表（坐标轴标签、图例、数字格式）的完整规范。** 只在 1.8 雷 10 和 3.3 里各提了一句。
5. **研究样本普遍偏小。** NN/g 中文研究 n=12、色调偏好研究的具体样本量未见公开、最小字号研究样本为中老年群体。这些结论应作为方向性参考，不是精确结论。
6. **PPT 正文尺寸的换算链路不够硬。** 1.4 节里「正文高度 ≈ 画面高度 3%～5%」是推算，不是标准原文；DISCAS 只给观看距离不给字号，中间那一步（视觉角度阈值）各家取值不一。**所以 1.4 给的最终建议是「默认 32px + 实测验证」，而不是一个公式。** 如果你需要一个能对客户或评审交代的数字，请去查 DISCAS 原文或做一次真机实测，不要直接引用本文档的推算过程。
7. **没有覆盖 PPT 的演讲者备注、讲义（handout）与无障碍阅读顺序。** HTML 版 PPT 的 DOM 顺序决定了读屏软件的朗读顺序，与视觉顺序不一致时会出问题，这块需要单独处理。
